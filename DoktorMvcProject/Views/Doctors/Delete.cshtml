@model DoktorMvcProject.Entities.Doctor

@{
    ViewBag.Title = "Delete Doctor";
}

<h2>@ViewBag.Title</h2>

@Html.Partial("_Details", Model)

<h3>Are you sure you want to delete this?</h3>


@using (Html.BeginForm("Delete", "Doctors", FormMethod.Post))
{
    @Html.HiddenFor(e => e.Id)
    <input type="submit" value="Yes" class="btn btn-success" />
    @:&nbsp;
    @Html.ActionLink("No", "Index", null, htmlAttributes: new { @class = "btn btn-danger" })
    <label class="text-success">@TempData["Info"]</label>//bunu açılır pencerede yapmayı unutmaaaaaaaa
}

